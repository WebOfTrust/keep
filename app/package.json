{
  "name": "keep",
  "version": "1.0.2",
  "description": "Electron based application for managing KERI identifiers",
  "author": {
    "name": "Kevin Griffin",
    "email": "griffin.kev@gmail.com"
  },
  "main": "index.js",
  "license": "Apache-2.0",
  "homepage": "https://github.com/weboftrust/keep",
  "devDependencies": {
    "electron": "17.1.0"
  },
  "dependencies": {
    "@todesktop/runtime": "^1.0.2",
    "cors": "^2.8.5",
    "electron-log": "^4.4.6",
    "express": "^4.18.1",
    "json": "^11.0.0",
    "node-fetch": "^3.2.3",
    "promise-retry": "^2.0.1"
  },
  "scripts": {
    "start": "electron .",
    "build": "yarn && todesktop build",
    "quick-build": "yarn && todesktop build . --code-sign=false",
    "release": "yarn && todesktop build && todesktop release --latest --force"
  }
}
