const Mailbox = require('./Mailbox');
const PresentationRequest = require('./PresentationRequest');
const Recipient = require('./Recipient');

module.exports = {
  Mailbox,
  PresentationRequest,
  Recipient,
};
